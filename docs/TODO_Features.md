# Missing / Desired Features (checkboxes)

Legend: `[-]` open, `[x]` implemented

- [-] Auto-crawl same-origin routes and capture artifacts (snapshots + responsive screenshots)
- [-] Visual regression: baselines + diff images + summary report
- [-] “True mobile” sweep option (device emulation: UA/touch/isMobile) rather than viewport-only
- [-] Auto-generate an HTML/Markdown report linking all screenshots/snapshots by route + viewport
- [-] First-class login helpers (optional): save storage state, detect “login required”, retry after auth

